<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="portal_service_list" name="Mis Servicios">
        <t t-call="portal.portal_layout">
            <div class="container my-4">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h2 class="m-0 text-primary">Mis Servicios de Custodia</h2>
                    <a href="/solicitar-servicio" class="btn btn-primary shadow-sm">
                        <i class="fa fa-plus me-1"/> Solicitar Servicio
                    </a>
                </div>

                <div class="table-responsive bg-white shadow-sm rounded">
                    <table class="table table-hover mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Folio</th>
                                <th>Ruta</th>
                                <th>Inicio</th>
                                <th>Nivel</th>
                                <th>Estado</th>
                                <th class="text-end">Acción</th>
                            </tr>
                        </thead>
                        <tbody>
                            <t t-if="services">
                                <t t-foreach="services" t-as="s">
                                    <tr class="align-middle">
                                        <td class="fw-bold"><t t-esc="s.sequence"/></td>
                                        <td><t t-esc="s.ruta_id.name"/></td>
                                        <td><t t-esc="s.start_datetime" t-options='{"widget": "datetime"}'/></td>
                                        <td><span class="badge border text-dark">Nivel <t t-esc="s.nivel_seguridad"/></span></td>
                                        <td>
                                            <span t-field="s.state" class="badge rounded-pill bg-info text-white"/>
                                        </td>
                                        <td class="text-end">
                                            <a t-att-href="'/mis-servicios/%s?access_token=%s' % (s.id, s.access_token)" 
                                               class="btn btn-sm btn-outline-secondary">
                                                Ver Detalle
                                            </a>
                                        </td>
                                    </tr>
                                </t>
                            </t>
                            <t t-else="">
                                <tr>
                                    <td colspan="6" class="text-center text-muted py-5">
                                        No existen servicios registrados.
                                    </td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                </div>
            </div>
        </t>
    </template>

    <template id="portal_service_form" name="Nueva Solicitud">
        <t t-call="portal.portal_layout">
            <div id="wrap" class="container my-4 bg-white p-4 shadow-sm rounded">
                <h2 class="mb-4 text-primary">Nueva Solicitud de Custodia</h2>
                <t t-if="error">
                    <div class="alert alert-danger shadow-sm" role="alert">
                        <i class="fa fa-exclamation-triangle me-2"/> <t t-esc="error"/>
                    </div>
                </t>
                <form action="/solicitar-servicio/submit" method="post" class="row g-3">
                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                    
                    <div class="col-md-12">
                        <label class="form-label fw-bold">Persona Solicitante</label>
                        <select name="contact_id" class="form-select" required="1">
                            <t t-foreach="contacts" t-as="c">
                                <option t-att-value="c.id"><t t-esc="c.display_name"/></option>
                            </t>
                        </select>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-bold">Carrier</label>
                        <select name="carrier_id" class="form-select" required="1">
                            <t t-foreach="carriers" t-as="car">
                                <option t-att-value="car.id"><t t-esc="car.name"/></option>
                            </t>
                        </select>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-bold">Ruta</label>
                        <select name="ruta_id" class="form-select" required="1">
                            <t t-foreach="rutas" t-as="r">
                                <option t-att-value="r.id"><t t-esc="r.name"/></option>
                            </t>
                        </select>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-bold">Inicio Estimado</label>
                        <input type="datetime-local" name="start_datetime" class="form-control" required="1"/>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-bold text-danger">Nivel de Seguridad *</label>
                        <select name="nivel_seguridad" class="form-select" required="1">
                            <option value="">-- Seleccione un nivel --</option>
                            <option value="1">Nivel 1 (Básico)</option>
                            <option value="2">Nivel 2 (Estándar)</option>
                            <option value="3">Nivel 3 (Reforzado)</option>
                            <option value="4">Nivel 4 (Alto Impacto)</option>
                            <option value="4x">Excepción + Nivel 4</option>
                        </select>
                        <div class="form-text">Este dato es requerido por el sistema de monitoreo.</div>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-bold">Load ID / Referencia</label>
                        <input type="text" name="load_id" class="form-control" placeholder="Ej. NKS221255" required="1"/>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-bold">Tipo de Unidad</label>
                        <input type="text" name="tipo_unidad" class="form-control" placeholder="Caja Seca, Plataforma..." required="1"/>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-bold">Placas</label>
                        <input type="text" name="placas" class="form-control" placeholder="ABC-123" required="1"/>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-bold">Transporte (Línea)</label>
                        <input type="text" name="transporte" class="form-control" placeholder="Nombre de la línea" required="1"/>
                    </div>

                    <div class="col-12 mt-4 text-end border-top pt-3">
                        <a href="/mis-servicios" class="btn btn-light me-2">Cancelar</a>
                        <button type="submit" class="btn btn-success btn-lg px-5 shadow-sm">
                            Registrar Solicitud
                        </button>
                    </div>
                </form>
            </div>
        </t>
    </template>

    <template id="portal_service_detail" name="Detalle del Servicio">
        <t t-call="portal.portal_layout">
            <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"/>
            <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>

            <div class="container my-4 bg-white pb-5 shadow-sm rounded">
                <div class="d-flex justify-content-between align-items-center pt-3 px-3">
                    <h2 class="mb-0 text-primary">Servicio <t t-esc="service.sequence"/></h2>
                    <span t-field="service.state" class="badge rounded-pill bg-info p-2 px-3" style="font-size: 0.9rem;"/>
                </div>
                <hr class="mx-3"/>

                <div class="row mt-4 px-3">
                    <div class="col-md-6">
                        <h5 class="text-muted border-bottom pb-2">Información de Ruta</h5>
                        <p><b>Ruta:</b> <t t-esc="service.ruta_id.name"/></p>
                        <p><b>Inicio:</b> <t t-esc="service.start_datetime"/></p>
                        <p><b>Nivel de Seguridad:</b> <span class="badge border text-dark">Nivel <t t-esc="service.nivel_seguridad"/></span></p>
                        <p><b>Load ID:</b> <t t-esc="service.load_id"/></p>
                    </div>
                    <div class="col-md-6 border-start">
                        <h5 class="text-muted border-bottom pb-2">Unidad y Operador</h5>
                        <ul class="list-unstyled">
                            <li><b>Unidad:</b> <span t-field="service.tipo_unidad"/></li>
                            <li><b>Placas:</b> <span t-field="service.placas"/></li>
                            <li><b>Operador:</b> <span t-field="service.operador1_nombre"/></li>
                        </ul>
                    </div>
                </div>

                <div class="row mt-5 px-3">
                    <div class="col-lg-6 mb-4">
                        <h4 class="text-secondary">Ruta Planeada</h4>
                        <div id="route-map" t-att-data-ruta-id="service.ruta_id.id" style="height:400px;" class="border rounded shadow-sm"></div>
                    </div>
                    <div class="col-lg-6 mb-4">
                        <h4 class="text-secondary">Monitoreo en Vivo</h4>
                        <div id="live-map" style="height:400px;" class="border rounded shadow-sm"></div>
                    </div>
                </div>

                <div id="service_communication" class="mt-4 pt-4 border-top px-3">
                    <h4 class="mb-4 text-muted">Bitácora de Seguimiento</h4>
                    <t t-call="portal.message_thread">
                        <t t-set="object" t-value="service"/>
                        <t t-set="token" t-value="service.access_token"/>
                    </t>
                </div>
            </div>

            <script type="text/javascript">
            <![CDATA[
            document.addEventListener("DOMContentLoaded", function () {
                // Mapa Ruta
                var rCont = document.getElementById("route-map");
                if (rCont && rCont.dataset.rutaId) {
                    fetch("/custodia/ruta/" + rCont.dataset.rutaId + "/coordinates", {
                        method: "POST",
                        headers: { "Content-Type": "application/json" },
                        body: JSON.stringify({ params: {} })
                    }).then(res => res.json()).then(data => {
                        if (data.result && data.result.length) {
                            var pts = data.result;
                            var rMap = L.map("route-map").setView([pts[0].lat, pts[0].lng], 6);
                            L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png").addTo(rMap);
                            var coords = pts.map(p => p.lng + "," + p.lat).join(";");
                            fetch("https://router.project-osrm.org/route/v1/driving/" + coords + "?overview=full&geometries=geojson")
                            .then(res => res.json()).then(osrm => {
                                if (osrm.routes && osrm.routes.length) {
                                    var layer = L.geoJSON(osrm.routes[0].geometry, { style: { color: "#3388ff", weight: 5 } }).addTo(rMap);
                                    rMap.fitBounds(layer.getBounds());
                                }
                            });
                        }
                    });
                }

                // Mapa Vivo
                var sId = "]]><t t-out='service.id'/><![CDATA[";
                var tok = "]]><t t-out='service.access_token'/><![CDATA[";
                var liveMap = L.map('live-map').setView([19.43, -99.13], 6);
                L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png").addTo(liveMap);
                var marker = L.marker([19.43, -99.13]).addTo(liveMap);

                function up() {
                    fetch("/mis-servicios/" + sId + "/tracking", {
                        method: "POST",
                        headers: { "Content-Type": "application/json" },
                        body: JSON.stringify({ params: { access_token: tok } })
                    }).then(res => res.json()).then(d => {
                        var r = d.result || d;
                        if (r.lat && r.lng) {
                            marker.setLatLng([r.lat, r.lng]);
                            liveMap.panTo([r.lat, r.lng]);
                        }
                    });
                }
                up(); setInterval(up, 15000);
            });
            ]]>
            </script>
        </t>
    </template>

    <template id="portal_my_home_custodia" inherit_id="portal.portal_my_home" name="Custodia Portal" priority="30">
        <xpath expr="//div[contains(@class,'o_portal_docs')]" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t t-set="title">Servicios de Custodia</t>
                <t t-set="url" t-value="'/mis-servicios'"/>
                <t t-set="placeholder_count" t-value="'services_count'"/>
            </t>
        </xpath>
    </template>

</odoo>
